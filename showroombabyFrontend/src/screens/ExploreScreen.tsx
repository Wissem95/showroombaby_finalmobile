import React, { useEffect } from 'react';
import { useFocusEffect } from '@react-navigation/native';

const ExploreScreen = () => {
  useEffect(() => {
    // Add any necessary focus effects here
  }, []);

  return (
    // Rest of the component code
  );
};

export default ExploreScreen; 